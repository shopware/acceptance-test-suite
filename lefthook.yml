pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,js,mjs}"
      run: npx oxlint --type-aware {staged_files}
    format:
      glob: "*.{ts,js,mjs,json,md}"
      run: npx oxfmt --write {staged_files}
      stage_fixed: true
