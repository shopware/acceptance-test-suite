pre-commit:
    parallel: true
    commands:
        lint:
            run: npx oxlint --type-aware
        format:
            glob: "*.{ts,js,mjs,json,md}"
            run: npx oxfmt --write {staged_files}
            stage_fixed: true
